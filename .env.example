# DMH 环境变量配置示例
# 复制此文件为 .env 并修改相应的值

# ==================== 数据库配置 ====================
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=dmh

# ==================== Redis 配置（可选） ====================
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ==================== 后端服务配置 ====================
API_PORT=8889
API_HOST=0.0.0.0

# JWT 密钥（生产环境请使用强密钥）
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRE=86400

# ==================== 前端配置 ====================
# 管理后台端口
ADMIN_PORT=3000

# H5 端口
H5_PORT=3100

# API 地址
VITE_API_BASE_URL=http://localhost:8889/api/v1

# ==================== 微信支付配置 ====================
WECHAT_APP_ID=your-app-id
WECHAT_APP_SECRET=your-app-secret
WECHAT_MCH_ID=your-mch-id
WECHAT_API_KEY=your-api-key

# ==================== 外部数据库同步配置 ====================
# Oracle 数据库
EXTERNAL_DB_TYPE=oracle
EXTERNAL_DB_HOST=external-db.example.com
EXTERNAL_DB_PORT=1521
EXTERNAL_DB_USER=username
EXTERNAL_DB_PASSWORD=password
EXTERNAL_DB_NAME=database_name

# SQL Server 数据库
# EXTERNAL_DB_TYPE=sqlserver
# EXTERNAL_DB_HOST=external-db.example.com
# EXTERNAL_DB_PORT=1433
# EXTERNAL_DB_USER=username
# EXTERNAL_DB_PASSWORD=password
# EXTERNAL_DB_NAME=database_name

# ==================== 文件存储配置 ====================
# 本地存储路径
STORAGE_PATH=./backend/storage

# OSS 配置（可选）
# OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
# OSS_ACCESS_KEY_ID=your-access-key-id
# OSS_ACCESS_KEY_SECRET=your-access-key-secret
# OSS_BUCKET=your-bucket-name

# ==================== 日志配置 ====================
LOG_LEVEL=info
LOG_PATH=./logs

# ==================== 其他配置 ====================
# 环境：development, production
ENV=development

# 是否启用调试模式
DEBUG=true

# CORS 允许的域名（多个用逗号分隔）
CORS_ORIGINS=http://localhost:3000,http://localhost:3100
